<TheoryScreen>:
    name: "theory_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - theory_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:


    MDBackTopBar:
        id: theory_top_bar
        title: "Теория"


<SchematicsScreen>:
    name: "schematics_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - schematics_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:


    MDBackTopBar:
        id: schematics_top_bar
        title: "Обозначения на схемах"


<PinoutScreen>:
    name: "pinout_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - pinout_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:


    MDBackTopBar:
        id: pinout_top_bar
        title: "Распиновки"


<ConnectionsScreen>:
    name: "connections_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - connections_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:


    MDBackTopBar:
        id: connections_top_bar
        title: "Типы соединений"


<ChipsScreen>:
    name: "chips_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - chips_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:


    MDBackTopBar:
        id: chips_top_bar
        title: "Микросхемы и компоненты"


<LifehacksScreen>:
    name: "lifehacks_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - lifehacks_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:


    MDBackTopBar:
        id: lifehacks_top_bar
        title: "Лайфхаки"
