<HowToScreeen>:
    name: "how_to_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - how_to_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:
                text:
                    """
                    Пользоваться приложением достаточно просто.
                    \n
                    В нижней части расположены вкладки для навигации по разделам. Внутри раздела навигация осуществляется по соответствующим ссылкам и с помощью кнопки "назад".
                    \n
                    Все значения выбираются или из выпадающего списка (как в маркировке резисторов), или вводятся с клавиатуры.
                    \n
                    ВАЖНО! Десятичным разделителем является точка! "0.5" - правильно, "0,5" - неправильно.
                    \n
                    Там, где это возможно, расчёт производится автоматически. Если результат не отобразился - нужно нажать кнопку "Рассчитать".
                    """

    MDBackTopBar:
        id: how_to_top_bar
        title: "Как пользоваться"


<AboutScreen>:
    name: "about_screen"
    md_bg_color: app.theme_cls.bg_dark

    MDRecycleView:
        size_hint_y: (root.height - sp(60)) / root.height
        pos_hint: {"top": (root.height - about_top_bar.height) / root.height}

        MDPaddedList:

            MDDumbLabel:
                text:
                    """
                    Идея программы родилась вместе с тем, как я увлёкся радиолюбительством.
                    Всегда было неудобно искать информацию в разных источниках и пользваться приложениями, где что-то есть, а чего-то нет. Поэтому я решил сделать своё приложение [s]с блэкджеком...[/s], где решил собрать всю необходимую информацию в одном месте.
                    \n
                    Пожелания и отзывы можно направить по почте [u][ref=mailto]electronics@hand-made-tlt.ru[/ref][/u].
                    """
                on_ref_press: root.mailto()

            MDDumbLabel:
                text:
                    """
                    [u][ref=git]Исходный код[/ref][/u]
                    """
                on_ref_press: root.git()
                halign: "center"

            MDDumbLabel:
                text:
                    """
                    Приложение доступно абсолютно безвозмездно.
                    Но при желании вы можете [u][ref=pay]поддержать автора[/ref][/u].
                    """
                on_ref_press: root.pay()

    MDTopAppBar:
        id: about_top_bar
        title: "О программе"
        pos_hint: {"top": 1}
        left_action_items: [["arrow-left", lambda x: app.back_to_screen()]]
        elevation: 1